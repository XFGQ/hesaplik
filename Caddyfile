{$DOMAIN:localhost} {
	encode gzip
	# Web ve API tek porttan: FastAPI hem /api'yi hem React build'ini
	# (imajın içindeki web/dist) servis eder. Caddy artık statik dosya
	# sunmaz; SPA fallback (bilinmeyen yol -> index.html) app/main.py'de.
	reverse_proxy api:8000
}
